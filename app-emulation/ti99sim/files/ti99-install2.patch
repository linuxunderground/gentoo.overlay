--- Makefile.linux-ori	2012-07-14 02:00:09.000000000 +0200
+++ Makefile.linux	2013-09-16 11:18:43.000000000 +0200
@@ -39,9 +39,11 @@
 	    $(INSTALL) -s $$target $(BIN_DIR); \
 	  done \
 	fi 
-	@for target in $(notdir $(TARGETS)); do \
+	@if [ $(BIN_DIR) != $(SYS_BIN) ]; then \
+	for target in $(notdir $(TARGETS)); do \
 	  ln -f -s $(BIN_DIR)/$$target $(SYS_BIN)/$$target; \
-	done
+	done \
+	fi
 
 uninstall:
 	@for target in $(notdir $(TARGETS)); do \
